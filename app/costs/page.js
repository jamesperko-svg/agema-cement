import { revalidatePath } from 'next/cache';
import { requireAuth } from '../../lib/auth';
import { db, loadToledo, calculateModel } from '../../lib/db';
import Shell from '../_shell';
async function saveCargo(formData){'use server';requireAuth();const s=db();const id=String(formData.get('id'));const payload={saleable_nt:Number(formData.get('saleable_nt')),material_fob_per_nt:Number(formData.get('material_fob_per_nt')),ocean_freight_per_nt:Number(formData.get('ocean_freight_per_nt')),demurrage_per_nt:Number(formData.get('demurrage_per_nt')),misc_per_nt:Number(formData.get('misc_per_nt')),tariff_per_nt:Number(formData.get('tariff_per_nt')),expected_arrival_date:String(formData.get('expected_arrival_date')||'')||null};const {error}=await s.from('cargoes').update(payload).eq('id',id);if(error)throw error;const customerId=String(formData.get('customer_id'));const {error:e2}=await s.from('customers').update({default_price_per_nt:Number(formData.get('sell_price'))}).eq('id',customerId);if(e2)throw e2;revalidatePath('/costs');revalidatePath('/dashboard')}
export default async function Costs(){requireAuth();const data=await loadToledo();const m=calculateModel(data);const c=data.cargo;const customer=data.customers[0]||{};return <Shell><h1 className="title">Toledo Cargo Economics</h1><p className="muted">Edit the current cargo assumptions. MidWest terminal contract rates remain stored separately in Supabase.</p><form action={saveCargo} className="form section"><input type="hidden" name="id" value={c.id}/><input type="hidden" name="customer_id" value={customer.id||''}/><div className="form-grid">{[['saleable_nt','Saleable cargo (NT)',c.saleable_nt],['material_fob_per_nt','Material FOB ($/NT)',c.material_fob_per_nt],['ocean_freight_per_nt','Ocean freight ($/NT)',c.ocean_freight_per_nt],['demurrage_per_nt','Demurrage ($/NT)',c.demurrage_per_nt],['misc_per_nt','Misc. CS/JSC/Duty ($/NT)',c.misc_per_nt],['tariff_per_nt','Tariff ($/NT)',c.tariff_per_nt],['sell_price','Available sell price ($/NT)',customer.default_price_per_nt||160]].map(([n,l,v])=><div className="field" key={n}><label>{l}</label><input name={n} type="number" step="0.01" defaultValue={v??0}/></div>)}<div className="field"><label>Cargo available at Toledo</label><input name="expected_arrival_date" type="date" defaultValue={c.expected_arrival_date||''}/></div></div><div style={{marginTop:16}}><button className="btn">Save cargo assumptions</button></div></form><div className="section card"><h2>Calculated Toledo cost</h2><table className="table"><tbody><tr><td>MidWest variable terminal charges</td><td>${m.variableNT.toFixed(2)}/NT</td></tr><tr><td>MidWest fixed vessel charges</td><td>${m.fixedNT.toFixed(2)}/NT</td></tr><tr><td>Dynamic storage</td><td>${m.storageNT.toFixed(2)}/NT</td></tr><tr><td>A/R financing</td><td>${m.arFinanceNT.toFixed(2)}/NT</td></tr><tr><th>Total modeled cost</th><th>${m.modeledCost.toFixed(2)}/NT</th></tr></tbody></table></div></Shell>}
