import { requireAuth } from '../../lib/auth';
import { loadToledo, calculateModel } from '../../lib/db';
import Shell from '../_shell';
function money(v){return `$${Number(v||0).toFixed(2)}`} function num(v){return Number(v||0).toLocaleString(undefined,{maximumFractionDigits:0})}
export default async function Dashboard(){requireAuth();const data=await loadToledo();const m=calculateModel(data);const soldPct=m.qty?Math.min(100,(m.totalShipped/m.qty)*100):0;const last=m.weeks.filter(w=>w.shipped>0).at(-1);return <Shell><div className="top"><div><h1 className="title">Toledo Executive Dashboard</h1><div className="muted">El Arish → MidWest Terminals → Available Ready-Mix</div></div></div><div className="grid"><div className="card"><div className="metric-label">Modeled Cost / NT</div><div className="metric">{money(m.modeledCost)}</div></div><div className="card"><div className="metric-label">Sell Price / NT</div><div className="metric">{money(m.sellPrice)}</div></div><div className="card"><div className="metric-label">Margin / NT</div><div className="metric">{money(m.marginNT)}</div></div><div className="card"><div className="metric-label">Saleable Cargo</div><div className="metric">{num(m.qty)} NT</div></div><div className="card"><div className="metric-label">Stored SOFR</div><div className="metric">{m.sofr.toFixed(2)}%</div></div><div className="card"><div className="metric-label">SOFR + Bank</div><div className="metric">{(m.sofr+m.spread).toFixed(2)}%</div></div></div><div className="section card"><div className="metric-label">Projected cargo movement under current throughput schedule</div><div className="bar"><div style={{width:`${soldPct}%`}}/></div><p>{num(m.totalShipped)} NT scheduled; {num(m.remaining)} NT remains unscheduled.</p></div><div className="two section"><div className="card"><h2>Cost stack</h2><table className="table"><tbody><tr><td>Base cargo + Toledo terminal</td><td>{money(m.base)}/NT</td></tr><tr><td>Dynamic warehouse storage</td><td>{money(m.storageNT)}/NT</td></tr><tr><td>Available A/R financing</td><td>{money(m.arFinanceNT)}/NT</td></tr><tr><th>Modeled total</th><th>{money(m.modeledCost)}/NT</th></tr></tbody></table></div><div className="card"><h2>Cash-cycle signals</h2><p><b>Available terms:</b> Net {m.customer.payment_terms_days||60}</p><p><b>Bank rate:</b> {(m.sofr+m.spread).toFixed(2)}%</p><p><b>Projected last collection:</b> {last?last.paymentDate.toLocaleDateString():'—'}</p><p><b>Projected margin:</b> {money(m.marginNT)}/NT</p></div></div></Shell>}
